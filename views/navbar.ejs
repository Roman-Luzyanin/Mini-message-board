<nav class="navbar">
    <ul>
        <% for (let i = 0; i < links.length; i++) { %>
            <li>
                <a href="<%= links[i].href %>" class="<%= links[i].href === path ? 'disable' : '' %>">
                    <span><%= links[i].text %></span>
                </a>
            </li>
        <% } %>
    </ul>
</nav>
